const router = require('express').Router();

router.get('/singin', (req, res) => {
  res.send('Getting into the app');
});

router.get('/singup', (req, res) => {
  res.send('Singing up into the app');
});

module.exports = router;
